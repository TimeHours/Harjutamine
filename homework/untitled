
CRUD
// Crate
// Read
Update
Delete

Array CRUD

["test",2,3,null]
console.log([])
let list = ["test",2,3,null]
let fruits = ["orange","banana","apple"]
// Read
list[0] // "test"
list[1] // 2
list[2] // 3
list[3] // null
list[4] // undefined

list.length // mitu elementi listis on
// viimane element
list  // undifined
// eelviimane element
list[5-1]     // null


// kuidas leida array sees element
// tagastab 




list.filter(elem => elem > 0) // 2,3
fruits.indexOf("pear") // -1
fruits.includes("pear") // false
list.filter(elem => elem > 0 ) // 2,3

// indexOf
// kui on 0,1,2 on see selles järjekorras

/*
array CURD (list, ,massiiv, array)
#region Create...
#region Read...
Update
*/
fruits.push ("kiwi")
fruits[4] = watermelon
list[4] ="not null"
// esimene number in positsioon/index
// teine number on mitu ära kustutada
// kolmas, neljas, viies jne on mida juurde lisada
fruits.splice(2,0,"plum") // lisab kolmandaks elementiks plum
// Delete
fruits.pop() // kustutab viimase elemendi ära
fruits.splice(2,1) 
