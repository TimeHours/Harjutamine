




//teine variant 
let sql = "INSERT INTO table (serial, name) VALUES "
for (let line of line) {
  let name = "asd", serial = "12354654"
  sql += "('"+ serial +"','"+name+"'),\n";
  }
fs.writeFile('fail.txt', sql)







fs = require('fs');
fs.readFile('/Users/MM-21-152/Desktop/LE.txt', 'utf8', function (err,data) {
  if (err) {
    return console.log(err);
  }
  fs.appendFile('fgal.txt', sql)
  data.replace("data","")
  .replace("data","").replace("data","")
  .replace("data","")

});











data.split("\n")

for (let line of Array) {
  let name = "Valjuveljed", serial = "00002356517"
  let sql = "('"+ serial +"','"+name+"'),\n";
  fs.appendFile('LE.txt', sql)
}